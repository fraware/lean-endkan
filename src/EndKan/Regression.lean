import EndKan.Tactics

/-!
Regression surface: this module is imported by `EndKan` so CI `lake build` verifies that the
public tactic layer still elaborates. Extend with small `example` proofs when adding tactics.
-/

example : True := trivial
